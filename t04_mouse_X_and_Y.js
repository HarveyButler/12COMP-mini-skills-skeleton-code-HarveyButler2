/********************************************************/
//
/********************************************************/

function setup() {
  createCanvas(400, 400);
   background(50);

}

function draw() {
 fill(200, 200, 250, 100)
    ellipse(mouseX, mouseY, 20, 20);
    noStroke()
}